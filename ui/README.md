# UI

Frontend/user-interface code for Halley.

**Status: not implemented.** The UI toolkit and process model are undecided
(see `docs/architecture.md`, "Future process model"). No UI exists; do not
add mock screens that pretend browser features are working.
